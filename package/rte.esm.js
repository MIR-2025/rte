/**
 * RTE — Rich Text Editor (ESM entry point)
 *
 * Usage:
 *   import RTE from 'rte-rich-text-editor';
 *   const editor = RTE.init('#editor');
 */
import RTE from './rte.js';
export default RTE;
export const init = RTE.init;
